from django.contrib import admin
from .models import FitProfile
# Register your models here.
#@admin.register(FitProfile)
#class FitProfileAdmin(admin.ModelAdmin):
#    list_display = ['user__username','fit_prof_phne','fit_prof_birth_date','fit_prof_height','fit_prof_weight']
#    search_fields = ['user__username']
    
